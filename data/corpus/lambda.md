# AWS Lambda

AWS Lambda is a serverless compute service that runs your function code in response to events without provisioning or managing servers.

You are billed only for the compute time your function consumes, measured in milliseconds. Lambda functions scale automatically with the number of incoming events and integrate with triggers such as API Gateway, S3, and DynamoDB streams.
