# Amazon RDS

Amazon RDS is a managed relational database service that handles provisioning, patching, backup, and recovery for engines like PostgreSQL and MySQL.

Multi-AZ deployments replicate data synchronously to a standby in another Availability Zone for failover, while read replicas scale read-heavy workloads horizontally.
