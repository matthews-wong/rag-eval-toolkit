# Amazon DynamoDB

Amazon DynamoDB is a fully managed NoSQL key-value and document database that delivers single-digit millisecond latency at any scale.

Tables use a partition key to distribute data across storage nodes. On-demand and provisioned capacity modes control throughput, and global tables replicate data across regions for low-latency multi-region access.
