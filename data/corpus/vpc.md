# Amazon VPC

A virtual private cloud (VPC) is a logically isolated virtual network in AWS where you launch resources into subnets you define.

You control the IP address range, subnets, route tables, and network gateways. Public subnets route to an internet gateway, while private subnets reach the internet through a NAT gateway. Security groups and network ACLs filter traffic at the instance and subnet level respectively.
