# AWS IAM

AWS Identity and Access Management (IAM) controls who is authenticated and authorized to use AWS resources.

Permissions are granted through policies attached to users, groups, and roles. IAM roles provide temporary credentials that services and federated identities can assume, following the principle of least privilege to limit access to only what is required.
